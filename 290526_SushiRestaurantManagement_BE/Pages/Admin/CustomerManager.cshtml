@page
@model _290526_SushiRestaurantManagement_BE.Pages.Admin.CustomerManagerModel
@using Services.Policies

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Quản Lý Khách Hàng";
}

@section Styles {
    <link rel="stylesheet" href="~/css/pages/admin-customer-manager.css" asp-append-version="true" />
}


<div class="customer-page">

    @if (TempData["Success"] != null)
    {
        <div class="alert admin-alert alert-success alert-dismissible fade show" role="alert">
            <i class="fa-solid fa-circle-check me-2"></i>
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert admin-alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fa-solid fa-triangle-exclamation me-2"></i>
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="customer-header">
        <div>
            <h2 class="customer-title">
                Quản Lý Khách Hàng
            </h2>
            <div class="customer-subtitle">
                Theo dõi thông tin, điểm tích lũy và trạng thái khách hàng
            </div>
        </div>

        <button type="button"
                class="btn-add-customer"
                data-bs-toggle="modal"
                data-bs-target="#addCustomerModal">
            <i class="fa-solid fa-user-plus me-1"></i>
            Thêm khách hàng
        </button>
    </div>

    <div class="search-card">
        <form method="get" class="row g-3 align-items-center">
            <div class="col-sm-9">
                <input type="text"
                       name="SearchString"
                       class="form-control search-input"
                       placeholder="Tìm theo tên hoặc số điện thoại"
                       value="@Model.SearchString" />
            </div>

            <div class="col-sm-3">
                <button type="submit" class="btn-search w-100">
                    Tìm kiếm
                </button>
            </div>
        </form>
    </div>

    <div class="customer-card">
        <div class="customer-card-header">
            <span>Danh sách khách hàng</span>
            <span class="customer-count-badge">
                @Model.CustomerList.Items.Count khách hàng
            </span>
        </div>

        <div class="table-responsive">
            <table class="table customer-table">
                <thead>
                    <tr>
                        <th class="ps-3">Mã KH</th>
                        <th>Họ và Tên</th>
                        <th>Số điện thoại</th>
                        <th>Hạng thành viên</th>
                        <th>Giảm giá</th>
                        <th>Điểm</th>
                        <th>Trạng thái</th>
                        <th class="text-center">Hành động</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var customer in Model.CustomerList.Items)
                    {
                        <tr>
                            <td class="ps-3 customer-id">
                                #@customer.CustomerId
                            </td>

                            <td class="customer-name">
                                @customer.FullName
                            </td>

                            <td>
                                <span class="phone-pill">
                                    @customer.Phone
                                </span>
                            </td>

                            <td>
                                <span class="membership-pill">
                                    @customer.MembershipLevel
                                </span>
                            </td>

                            
                            <td>
                                <span class="points-pill">
                                    @LoyaltyPolicy.GetDiscountPercent(customer.MembershipLevel).ToString("0.##")%
                                </span>
                            </td>

                            <td>
                                <span class="points-pill">
                                    @customer.LoyaltyPoints điểm
                                </span>
                            </td>

                            <td>
                                @if (customer.DeletedAt == null)
                                {
                                    <span class="status-pill status-active">
                                        Active
                                    </span>
                                }
                                else
                                {
                                    <span class="status-pill status-deleted">
                                        Deleted
                                    </span>
                                }
                            </td>

                            <td class="text-center">
                                <div class="action-group">
                                    <a asp-page="/Admin/EditCustomer"
                                       asp-route-id="@customer.CustomerId"
                                       class="btn-edit">
                                        Sửa
                                    </a>

                                    <form method="post"
                                          asp-page-handler="Delete"
                                          asp-route-id="@customer.CustomerId"
                                          onsubmit="return confirm('Bạn có chắc chắn muốn xóa khách hàng này?');"
                                          class="d-inline">
                                        <button type="submit" class="btn-delete">
                                            Xóa
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }

                    @if (!Model.CustomerList.Items.Any())
                    {
                        <tr>
                            <td colspan="8" class="text-center empty-row">
                                Không tìm thấy khách hàng nào.
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

</div>

<div class="modal fade customer-modal" id="addCustomerModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" asp-page-handler="AddCustomer">
                <div class="modal-header">
                    <h5 class="modal-title">
                        Thêm khách hàng mới
                    </h5>

                    <button type="button"
                            class="btn-close btn-close-white"
                            data-bs-dismiss="modal"
                            aria-label="Close"></button>
                </div>

                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label">Họ và tên</label>
                        <input type="text"
                               name="NewFullName"
                               class="form-control"
                               required
                               placeholder="Nhập họ và tên" />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Số điện thoại</label>
                        <input type="text"
                               name="NewPhone"
                               class="form-control"
                               required
                               placeholder="Nhập số điện thoại" />
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button"
                            class="btn-modal-cancel"
                            data-bs-dismiss="modal">
                        Hủy
                    </button>

                    <button type="submit"
                            class="btn-modal-save">
                        Tạo khách hàng
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>